# Production runtime dependencies for OrbitWatcher.
# Research/plotting dependencies such as matplotlib are intentionally
# excluded because they are not required by the deployed application.

fastapi==0.138.0
uvicorn==0.49.0
requests==2.34.2
sgp4==2.25
scipy==1.18.0
numpy==2.5.0
apscheduler==3.11.2
python-dotenv==1.2.2